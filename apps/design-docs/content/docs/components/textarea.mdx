---
title: Textarea
description: A multi-line text input. Defaults to vertical resize only.
---

import { Textarea, Label } from '@/components/preview'

<Preview align="start">
  <div style={{ display: 'flex', flexDirection: 'column', gap: 6, width: '100%', maxWidth: 360 }}>
    <Label htmlFor="bio">Bio</Label>
    <Textarea id="bio" placeholder="Tell us a bit about yourself..." rows={4} />
  </div>
</Preview>

## States

<Preview align="start">
  <div style={{ display: 'flex', flexDirection: 'column', gap: 8, width: '100%', maxWidth: 360 }}>
    <Textarea invalid rows={3} placeholder="This field has an error" />
    <Textarea disabled rows={3} placeholder="Read only" />
    <Textarea resize="none" rows={3} placeholder="Fixed height" />
  </div>
</Preview>

## Usage

```tsx
import { Textarea, Label } from '@onersoft/design-system'

<Label htmlFor="bio">Bio</Label>
<Textarea id="bio" rows={4} placeholder="…" />
```

## Props

| Prop | Type | Default |
| ---- | ---- | ------- |
| `invalid` | `boolean` | `false` |
| `resize` | `'none' \| 'vertical' \| 'horizontal' \| 'both'` | `'vertical'` |

Plus all native `<textarea>` attributes (`rows`, `placeholder`, `disabled`, `value`, `onChange`, …).
